package week3;

public class MinInitialValue {

	/**
	 * Compute the initial battery level required to complete the trajectory
	 * given by s (as specified in the workshop question) with the constraint
	 * that the battery capacity is limited to maxLevel
	 * 
	 * @param s			the list of trajectories, negative value is a descent
	 * 					and positive value is an ascent
	 * @param maxLevel	the maximum energy that can be stored in the battery
	 * @return
	 */
	public static int findInitialValue(int[] s, int maxLevel) {
		int initialLevel = 0;

		return initialLevel;
	}
}